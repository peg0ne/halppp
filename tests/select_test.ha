fn main =>
    select a from b doremi a
    select c from b dore c
    select d from c do b
    select a from d
        select a from b doremi a
        select c from b dore c
        select d from c do b
        select a from d
            print("Hello")
        ;
    ;
;
